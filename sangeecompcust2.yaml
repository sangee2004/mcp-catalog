name: Sangee Composite Customized 2
runtime: composite
compositeConfig:
  componentServers:
    - catalogEntryID: github.com/obot-platform/mcp-catalog::obot-gmail
      toolPrefix: my_configured_
      toolOverrides:
         - name: delete_email
          enabled: false
