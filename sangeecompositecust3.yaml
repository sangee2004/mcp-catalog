name: Sangee Composite Customized EnabledFalseAndTrue
runtime: composite
compositeConfig:
  componentServers:
    - catalogEntryID: github.com/obot-platform/mcp-catalog::obot-gmail
      toolPrefix: my_configured_
      toolOverrides:
         - name: delete_email
           enabled: false
         - name: read_email
           enabled: true
         - name: list_email
           enabled: true
